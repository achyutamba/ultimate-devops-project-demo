apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-demo.fullname" . }}-frontendproxy
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: frontendproxy
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-frontendproxy
spec:
  type: {{ .Values.frontendproxy.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.frontendproxy.service.port | default 8080 }}
      targetPort: {{ .Values.frontendproxy.service.targetPort | default 8080 }}
      name: tcp-service
  selector:
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-frontendproxy