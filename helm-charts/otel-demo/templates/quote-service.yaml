apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-demo.fullname" . }}-quoteservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: quoteservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-quoteservice
spec:
  type: {{ .Values.quote.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.quote.service.port | default 8080 }}
      targetPort: {{ .Values.quote.service.targetPort | default 8080 }}
      name: tcp-service
  selector:
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-quoteservice