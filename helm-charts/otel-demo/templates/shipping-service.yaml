apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-demo.fullname" . }}-shippingservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: shippingservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-shippingservice
spec:
  type: {{ .Values.shipping.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.shipping.service.port | default 8080 }}
      targetPort: {{ .Values.shipping.service.targetPort | default 8080 }}
      name: tcp-service
  selector:
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-shippingservice