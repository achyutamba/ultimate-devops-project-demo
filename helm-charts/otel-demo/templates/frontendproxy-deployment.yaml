apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo.fullname" . }}-frontendproxy
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: frontendproxy
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-frontendproxy
spec:
  replicas: {{ .Values.frontendproxy.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-frontendproxy
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: frontendproxy
        app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-frontendproxy
      {{- if or .Values.global.podAnnotations .Values.frontendproxy.podAnnotations }}
      annotations:
        {{- with .Values.global.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.frontendproxy.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName | default "otel-demo" }}
      {{- with .Values.global.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: frontendproxy
          image: {{ .Values.frontendproxy.image.repository }}:{{ .Values.frontendproxy.image.tag }}
          imagePullPolicy: {{ .Values.frontendproxy.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.frontendproxy.service.port | default 8080 }}
              name: service
          {{- $svcSec := .Values.frontendproxy.securityContext | default dict }}
          {{- $globalSec := .Values.global.containerSecurityContext | default dict }}
          {{- $sec := $svcSec | default $globalSec }}
          {{- if $sec }}
          securityContext:
            {{- toYaml $sec | nindent 12 }}
          {{- end }}
          {{- $svcRes := .Values.frontendproxy.resources | default dict }}
          {{- $globalRes := .Values.global.resources | default dict }}
          {{- $res := $svcRes | default $globalRes }}
          {{- if $res }}
          resources:
            {{- toYaml $res | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.frontendproxy.env }}
            {{- toYaml .Values.frontendproxy.env | nindent 12 }}
            {{- end }}