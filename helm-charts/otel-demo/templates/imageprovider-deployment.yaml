apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo.fullname" . }}-imageprovider
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: imageprovider
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-imageprovider
spec:
  replicas: {{ .Values.imageprovider.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-imageprovider
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: imageprovider
        app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-imageprovider
      {{- if or .Values.global.podAnnotations .Values.imageprovider.podAnnotations }}
      annotations:
        {{- with .Values.global.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.imageprovider.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName | default "otel-demo" }}
      {{- with .Values.global.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: imageprovider
          image: {{ .Values.imageprovider.image.repository }}:{{ .Values.imageprovider.image.tag }}
          imagePullPolicy: {{ .Values.imageprovider.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.imageprovider.service.port | default 8081 }}
              name: service
          {{- $svcSec := .Values.imageprovider.securityContext | default dict }}
          {{- $globalSec := .Values.global.containerSecurityContext | default dict }}
          {{- $sec := $svcSec | default $globalSec }}
          {{- if $sec }}
          securityContext:
            {{- toYaml $sec | nindent 12 }}
          {{- end }}
          {{- $svcRes := .Values.imageprovider.resources | default dict }}
          {{- $globalRes := .Values.global.resources | default dict }}
          {{- $res := $svcRes | default $globalRes }}
          {{- if $res }}
          resources:
            {{- toYaml $res | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.imageprovider.env }}
            {{- toYaml .Values.imageprovider.env | nindent 12 }}
            {{- end }}