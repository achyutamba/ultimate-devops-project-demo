apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-demo.fullname" . }}-emailservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: emailservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-emailservice
spec:
  type: {{ .Values.email.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.email.service.port | default 8080 }}
      targetPort: {{ .Values.email.service.targetPort | default 8080 }}
      name: tcp-service
  selector:
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-emailservice