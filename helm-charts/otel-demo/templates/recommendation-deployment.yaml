apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo.fullname" . }}-recommendationservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: recommendationservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-recommendationservice
spec:
  replicas: {{ .Values.recommendation.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-recommendationservice
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: recommendationservice
        app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-recommendationservice
      {{- if or .Values.global.podAnnotations .Values.recommendation.podAnnotations }}
      annotations:
        {{- with .Values.global.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.recommendation.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName | default "otel-demo" }}
      {{- with .Values.global.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: recommendationservice
          image: {{ .Values.recommendation.image.repository }}:{{ .Values.recommendation.image.tag }}
          imagePullPolicy: {{ .Values.recommendation.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.recommendation.service.port | default 8080 }}
              name: service
          {{- $svcSec := .Values.recommendation.securityContext | default dict }}
          {{- $globalSec := .Values.global.containerSecurityContext | default dict }}
          {{- $sec := $svcSec | default $globalSec }}
          {{- if $sec }}
          securityContext:
            {{- toYaml $sec | nindent 12 }}
          {{- end }}
          {{- $svcRes := .Values.recommendation.resources | default dict }}
          {{- $globalRes := .Values.global.resources | default dict }}
          {{- $res := $svcRes | default $globalRes }}
          {{- if $res }}
          resources:
            {{- toYaml $res | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.recommendation.env }}
            {{- toYaml .Values.recommendation.env | nindent 12 }}
            {{- end }}