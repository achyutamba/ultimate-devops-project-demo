apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo.fullname" . }}-quoteservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: quoteservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-quoteservice
spec:
  replicas: {{ .Values.quote.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-quoteservice
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: quoteservice
        app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-quoteservice
      {{- if or .Values.global.podAnnotations .Values.quote.podAnnotations }}
      annotations:
        {{- with .Values.global.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.quote.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName | default "otel-demo" }}
      {{- with .Values.global.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: quoteservice
          image: {{ .Values.quote.image.repository }}:{{ .Values.quote.image.tag }}
          imagePullPolicy: {{ .Values.quote.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.quote.service.port | default 8080 }}
              name: service
          {{- $svcSec := .Values.quote.securityContext | default dict }}
          {{- $globalSec := .Values.global.containerSecurityContext | default dict }}
          {{- $sec := $svcSec | default $globalSec }}
          {{- if $sec }}
          securityContext:
            {{- toYaml $sec | nindent 12 }}
          {{- end }}
          {{- $svcRes := .Values.quote.resources | default dict }}
          {{- $globalRes := .Values.global.resources | default dict }}
          {{- $res := $svcRes | default $globalRes }}
          {{- if $res }}
          resources:
            {{- toYaml $res | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.quote.env }}
            {{- toYaml .Values.quote.env | nindent 12 }}
            {{- end }}