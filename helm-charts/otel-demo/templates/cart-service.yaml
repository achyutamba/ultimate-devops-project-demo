apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-demo.fullname" . }}-cartservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: cartservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-cartservice
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    app.kubernetes.io/part-of: otel-demo
spec:
  type: {{ .Values.cart.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.cart.service.port | default 8080 }}
      name: tcp-service
      targetPort: {{ .Values.cart.service.targetPort | default 8080 }}
  selector:
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-cartservice
