apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo.fullname" . }}-shippingservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: shippingservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-shippingservice
spec:
  replicas: {{ .Values.shipping.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-shippingservice
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: shippingservice
        app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-shippingservice
      {{- if or .Values.global.podAnnotations .Values.shipping.podAnnotations }}
      annotations:
        {{- with .Values.global.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.shipping.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName | default "otel-demo" }}
      {{- with .Values.global.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: shippingservice
          image: {{ .Values.shipping.image.repository }}:{{ .Values.shipping.image.tag }}
          imagePullPolicy: {{ .Values.shipping.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.shipping.service.port | default 8080 }}
              name: service
          {{- $svcSec := .Values.shipping.securityContext | default dict }}
          {{- $globalSec := .Values.global.containerSecurityContext | default dict }}
          {{- $sec := $svcSec | default $globalSec }}
          {{- if $sec }}
          securityContext:
            {{- toYaml $sec | nindent 12 }}
          {{- end }}
          {{- $svcRes := .Values.shipping.resources | default dict }}
          {{- $globalRes := .Values.global.resources | default dict }}
          {{- $res := $svcRes | default $globalRes }}
          {{- if $res }}
          resources:
            {{- toYaml $res | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.shipping.env }}
            {{- toYaml .Values.shipping.env | nindent 12 }}
            {{- end }}