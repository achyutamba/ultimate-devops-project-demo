apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo.fullname" . }}-currencyservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: currencyservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-currencyservice
spec:
  replicas: {{ .Values.currency.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-currencyservice
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: currencyservice
        app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-currencyservice
      {{- if or .Values.global.podAnnotations .Values.currency.podAnnotations }}
      annotations:
        {{- with .Values.global.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.currency.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName | default "otel-demo" }}
      {{- with .Values.global.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: currencyservice
          image: {{ .Values.currency.image.repository }}:{{ .Values.currency.image.tag }}
          imagePullPolicy: {{ .Values.currency.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.currency.service.port | default 8080 }}
              name: service
          {{- $svcSec := .Values.currency.securityContext | default dict }}
          {{- $globalSec := .Values.global.containerSecurityContext | default dict }}
          {{- $sec := $svcSec | default $globalSec }}
          {{- if $sec }}
          securityContext:
            {{- toYaml $sec | nindent 12 }}
          {{- end }}
          {{- $svcRes := .Values.currency.resources | default dict }}
          {{- $globalRes := .Values.global.resources | default dict }}
          {{- $res := $svcRes | default $globalRes }}
          {{- if $res }}
          resources:
            {{- toYaml $res | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.currency.env }}
            {{- toYaml .Values.currency.env | nindent 12 }}
            {{- end }}