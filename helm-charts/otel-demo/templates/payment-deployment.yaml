apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo.fullname" . }}-paymentservice
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: paymentservice
    app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-paymentservice
spec:
  replicas: {{ .Values.payment.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-paymentservice
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: paymentservice
        app.kubernetes.io/name: {{ include "otel-demo.fullname" . }}-paymentservice
      {{- if or .Values.global.podAnnotations .Values.payment.podAnnotations }}
      annotations:
        {{- with .Values.global.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.payment.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      serviceAccountName: {{ .Values.serviceAccountName | default "otel-demo" }}
      {{- with .Values.global.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: paymentservice
          image: {{ .Values.payment.image.repository }}:{{ .Values.payment.image.tag }}
          imagePullPolicy: {{ .Values.payment.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.payment.service.port | default 8080 }}
              name: service
          {{- $svcSec := .Values.payment.securityContext | default dict }}
          {{- $globalSec := .Values.global.containerSecurityContext | default dict }}
          {{- $sec := $svcSec | default $globalSec }}
          {{- if $sec }}
          securityContext:
            {{- toYaml $sec | nindent 12 }}
          {{- end }}
          {{- $svcRes := .Values.payment.resources | default dict }}
          {{- $globalRes := .Values.global.resources | default dict }}
          {{- $res := $svcRes | default $globalRes }}
          {{- if $res }}
          resources:
            {{- toYaml $res | nindent 12 }}
          {{- end }}
          env:
            {{- if .Values.payment.env }}
            {{- toYaml .Values.payment.env | nindent 12 }}
            {{- end }}